name: hostel_entry_authorization
version: 1.0.0+1
environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.2.2
  provider: ^6.1.2
  intl: ^0.19.0
  qr_flutter: ^4.1.0
  zxing2: ^0.2.3
  mobile_scanner: ^5.2.3

flutter:
  uses-material-design: true
